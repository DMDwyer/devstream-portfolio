services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: devstream
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports: ["5432:5432"]
    volumes: ["pg:/var/lib/postgresql/data"]
volumes: { pg: {} }
